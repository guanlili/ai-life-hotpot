# 大模型 API key —— 构建时由 Vite 烤进前端 bundle(VITE_ 前缀)。
#
# ⚠️ 因此 key 会出现在公开的 JS bundle 中,仅适合额度受限的一次性 demo key。
#
# 本地使用:
#   1. 复制本文件为 .env  (cp .env.example .env)
#   2. 在 .env 里填入你的 key
#   .env 已被 .gitignore 排除,不会提交到仓库。
#
# 线上(Cloudflare Workers git 部署):
#   在 Worker 的 Settings → Variables 里新增同名变量 VITE_LLM_KEY(构建变量),
#   这样 Cloudflare 构建时也能把 key 烤进 bundle。
VITE_LLM_KEY=
